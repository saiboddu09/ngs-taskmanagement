{
  "name": "task-management",
  "version": "1.0.0",
  "description": "The app is used to track the task of the user",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "./node_modules/.bin/eslint --ignore-path .gitignore . *.js"
  },
  "dependencies": {
    "express": "*"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
